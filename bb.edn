{:paths ["src"]
 :deps {cheshire/cheshire {:mvn/version "5.13.0"}}
 :tasks {itter {:doc "Fetch tweets from a user"
                :requires ([bbitter.cli :as cli])
                :task (apply cli/-main *command-line-args*)}}}
